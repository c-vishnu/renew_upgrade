name: Deploy to VM

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy
        run: bash /home/devuser/projects/renew_upgrade/scripts/deploy.sh
